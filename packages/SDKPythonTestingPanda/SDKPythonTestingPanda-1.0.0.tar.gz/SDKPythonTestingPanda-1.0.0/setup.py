"""
Python SDL external package setup
"""
from setuptools import setup, find_packages

setup(
    name='SDKPythonTestingPanda',
    version='1.0.0',
    description='Python SDK for Panda Server',
    packages=find_packages(),
    install_requires=[
    ],
)
