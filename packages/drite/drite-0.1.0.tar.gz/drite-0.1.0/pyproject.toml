[tool.poetry]
name = "drite"
version = "0.1.0"
description = ""
authors = ["Jeremy Berman <jerber@sas.upenn.edu>"]
readme = "README.md"

[tool.poetry.scripts]
#whitecloak = "whitecloak.main:app"
drite = "drite.main:app"

[tool.poetry.dependencies]
python = "^3.10"
typer = { extras = ["all"], version = "^0.12.3" }
httpx = "^0.27.0"
devtools = "^0.12.2"
pydantic = "^2.7.0"

[tool.poetry.dev-dependencies]
black = { version = "^22.3" }


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"