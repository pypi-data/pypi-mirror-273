[tool.poetry]
name = "docmake"
version = "0.1.0"
description = ""
authors = ["admin"]
readme = "README.md"
packages = [{include = "src"}]
repository = "https://github.com/morisono/docmaker-webui"

[tool.poetry.scripts]
docmake = 'src.cli:main'

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
