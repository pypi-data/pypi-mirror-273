[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "svasir03_ode_module"
version = "0.0.3"
authors = [
  { name="Svante Sirviö", email="svante.sirvio03@gmail.com" },
]
description = "ODE solver"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
  "numpy>=1.26"
]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]