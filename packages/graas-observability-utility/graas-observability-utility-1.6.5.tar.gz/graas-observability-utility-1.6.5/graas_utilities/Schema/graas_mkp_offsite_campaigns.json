{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Offsite Campaign Report",
    "description": "A JSON object representing a Offsite campaign report for a specific campaign.",
    "type": "object",
    "properties": {
        "merchantID": {
            "type": "string",
            "description": "A unique merchant code for GRAAS clients"
        },
        "siteNickNameId": {
            "type": "string",
            "description": "A unique merchant's store code for GRAAS clients"
        },
        "countryCode": {
            "type": "string",
            "description": "The country code, e.g., 'SG', 'MY'."
        },
        "currencyCode": {
            "type": "string",
            "description": "The currency code, e.g., 'SGD', 'USD'."
        },
        "result": {
            "type": "array",
            "items": {
                "anyOf": [
                    {
                        "$ref": "../graas_utilities/Schema/Lazada_offsite_campaign_Daily_Report.json",
                        "description": "Lazada Offsite Campaign Report"
                    }
                ]
            }
        }
    },
    "required": [
        "merchantID",
        "siteNickNameId",
        "countryCode",
        "currencyCode",
        "result"
    ]
}