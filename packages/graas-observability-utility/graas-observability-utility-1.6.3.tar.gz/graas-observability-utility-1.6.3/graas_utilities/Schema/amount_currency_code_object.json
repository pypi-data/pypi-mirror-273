{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Amount and Currency Code Object",
  "description": "A JSON schema for representing an object with an amount and its associated currency code, commonly used in financial and monetary contexts.",
  "type": "object",
  "properties": {
    "amount": {
      "type": "number",
      "description": "The cost amount per conversion."
    },
    "currencyCode": {
      "type": "string",
      "description": "The currency code, e.g., 'SGD', 'USD'."
    }
  },
  "required": [
    "amount",
    "currencyCode"
  ]
}