from setuptools import setup, find_packages


setup(
    name="common_hms_library",
    version="1.0",
    packages= find_packages(),
    description="This library is use to calculate in the form of float age on the basis of date of birth.",
    install_requires=[],
)