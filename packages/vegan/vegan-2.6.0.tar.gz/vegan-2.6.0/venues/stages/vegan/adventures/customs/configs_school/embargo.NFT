#!/usr/sbin/nft -f



table inet filter {
    chain input {
        type filter hook input priority 0;

        # Allow port 80
        tcp dport 80 accept
        
        # Allow port 443
        tcp dport 443 accept

        # Turn away the rest of the traffic
        drop
    }
}
