[metadata]
name = sphinx-inventory-builder
description = Adds a custom inventory builder to Sphinx
version = attr: sphinx_inventory_builder.__version__

[options]
zip_safe = false
python_requires = >=3.8
packages = find_namespace:
include_package_data = True
install_requires = 
	sphinx >= 4.0

[options.packages.find]
where = .
include = sphinx_inventory_builder*

[egg_info]
tag_build = 
tag_date = 0

