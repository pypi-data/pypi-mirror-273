[tool.poetry]
name = "bball-sim-v2"
version = "0.1.0"
description = ""
authors = ["jacob <jacob.johannsson@quantumbridgetech.com>"]
readme = "README.md"

[tool.poetry.scripts]
bball-sim-v2 = "bball_sim_v2.main:app"

[tool.poetry.dependencies]
python = "^3.10"
typer = {extras = ["all"], version = "^0.12.3"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
