[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "counterfusion"
version = "0.1.0"
description = ""
authors = [
    {name = "Lixian Wang", email = "lixianphys@gmail.com"}
]
dependencies = [
    "numpy==1.21.0",
    "pandas==1.3.0",
    "matplotlib>=3.6.0",
]

[tool.setuptools.packages.find]
where = ["counterfusion"]
