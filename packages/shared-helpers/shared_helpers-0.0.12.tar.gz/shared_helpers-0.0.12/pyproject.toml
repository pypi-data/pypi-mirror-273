[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "shared_helpers"
version = "0.0.12"
authors = [
 { name="", email="vladimir.dolzhikov1@leroymerlin.ru" },
]
description = ""
readme = "README.md"
requires-python = ">=3.10.0"
classifiers = [
   "Programming Language :: Python :: 3",
   "License :: OSI Approved :: MIT License",
   "Operating System :: OS Independent",
]

[tool.hatch.build.targets.wheel]
packages = ["src/shared_helpers"]